'use client';

import { ScheduleManager } from '@/components/schedules/schedule-manager';

export default function SchedulesAssignmentsPage() {
  return <ScheduleManager activeTab="assignments" />;
}
